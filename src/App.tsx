import React from 'react';

interface AppProps {}

const App: React.FC<AppProps> = () => {
	return (
		<div>
			<h1 className="text-white">Welcome to a new react project</h1>
		</div>
	);
};

export default App;
